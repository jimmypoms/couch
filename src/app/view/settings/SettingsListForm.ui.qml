import QtQuick 2.4
import "../components"

MenuPage {
    id: control

    property variant settings: QtObject {}

    title: qsTr("not yet supported")
    description: qsTr("setting are not yet supported :(")
}
